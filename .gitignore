# ===================================
# FINCA CHEONA - Git Ignore
# ===================================

# Node modules (solo los módulos instalados, no el package.json)
node_modules/
*/node_modules/

# Python (solo archivos compilados y entornos virtuales)
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
ENV/
.venv/

# Variables de entorno (solo archivos .env reales, no ejemplos)
.env
back_cheona_nuevo/.env
front_nuevo/.env
!.env.example
!back_cheona_nuevo/.env.example
!front_nuevo/.env.example

# Archivos temporales del sistema
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs y archivos temporales
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# Build output (solo archivos generados, no código fuente)
dist/
front_nuevo/build/
coverage/
*.lcov

# IDEs (configuraciones personales)
.vscode/settings.json
.vscode/launch.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# Docker (solo volúmenes y archivos temporales)
.docker/

# Archivos temporales del proyecto
update_chars.sql
*.tmp
*.backup

# Base de datos local (solo archivos de desarrollo local)
*.sqlite
*.sqlite3

# Uploads de desarrollo (mantener estructura pero no archivos subidos)
uploads/*
!uploads/.gitkeep
!uploads/README.md
